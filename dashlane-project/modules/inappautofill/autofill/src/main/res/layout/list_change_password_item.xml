<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@color/container_agnostic_neutral_supershy"
    android:minWidth="300dp"
    android:orientation="vertical"
    android:theme="@style/Theme.Dashlane"
    >

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:background="?dividerHorizontal"
        />

    <TextView
        android:id="@+id/line1TextView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:drawableStart="@drawable/ic_autofill_change_password"
        android:drawablePadding="@dimen/spacing_small"
        android:gravity="center_vertical"
        android:maxLines="1"
        android:paddingStart="@dimen/spacing_normal"
        android:paddingTop="@dimen/spacing_normal"
        android:paddingEnd="@dimen/spacing_normal"
        android:paddingBottom="@dimen/spacing_normal"
        android:singleLine="true"
        android:text="@string/autofill_change_password_popup_title"
        android:textAppearance="?textAppearanceButton"
        android:textColor="?attr/colorSecondary"
        />

</LinearLayout>